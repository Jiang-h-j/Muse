# Muse PRD V1 · 技术附录（Addendum）

- 关联主文档：[Muse-PRD-V1.md](Muse-PRD-V1.md)
- 来源：[PRFAQ Verdict（GO 附条件）](_bmad-output/planning-artifacts/prfaq-Muse.md) Stage 4/5
- 日期：2026-07-20
- 定位：本附录承载 PRD 正文**刻意不写**的技术实现路径（technical-how）、备选方案取舍、成本结构与残留待定项，供架构阶段（`bmad-create-architecture`）消费。PRD 正文只讲「要什么能力」，本附录讲「大致怎么建、为什么这么建、代价是什么」。

---

## A. 长程一致性机制迁移（对应模块 2 设定圣经 / 模块 4 归档）

**决策（PRFAQ Q1）**：直接采用开源项目 `webnovel-writer`（v6.2.1，Trendshift 上榜）的 Story System 一致性机制，作为模块 2「设定圣经」+ 模块 4「章节卡片/归档」的实现骨架。从「赌它能成」转为「知道怎么建」。

**机制闭环（三段）：**

1. **写前**：context-agent 把「设定圣经（story_contracts）+ 最近章节摘要 + 未回收伏笔（urgent_loops）+ 世界规则 + 主角状态」压成写作任务书，喂给正文生成。
2. **写后**：data-agent 从正文提取事件 / 状态变化 / 新增实体 → 结构化 JSON → 一次 chapter-commit 投影回 state / index / summary / memory / vector 各存储面。
3. **RAG 召回**：BM25 + 向量 + rerank 三级检索按相关性召回历史设定 / 情节，可选增强；无 embedding key 时退回 BM25。

**边界与清醒认知（已与用户确认）：**

- **(a) 机制照搬、章数不设人为上限**：原样采用其为「几百章」设计的机制，实现时**不得人为砍成 20–50 章**。20–50 章只是「文学向用户单本大致长度」的参照系（用于判断该机制在 Muse 场景够用），**不是产品硬上限**。
- **(b) 借思路 + 借部分代码，非装上即用**：webnovel-writer 是 Python + 跑在 Claude Code 内的单机单书 skill 框架、以文件系统当库；Muse 是多用户 Web 产品，**存储层 / 调度需为多用户后端重写**，此工作量 PRD 正文未计入，须在架构阶段估算。
- **(c) 残留风险绕回文字质量**：一致性依赖 data-agent「读正文提取新事实」，该提取环节仍由 LLM（DeepSeek）完成——提取漏 / 错则后续穿帮。故一致性的可靠性上限受模型质量（见 B 节）牵制，非独立可保。
- **许可证注意**：webnovel-writer 为 GPL 项目，迁移代码前须评估许可证义务与 Muse 商业形态的兼容性。

---

## B. 模型选型、文字质量与去 AI 味（对应模块 3 创作 / 主文档 §七 红线）

**决策（PRFAQ Q2 = A2）**：坚持用 DeepSeek 起草正文，暂不做前置验证测试。**诚实定性：这是全项目头号未验证生死假设**——「DeepSeek 在去 AI 味规则约束下能否产出过行为红线的文字」目前是赌，不是知。

**风险收窄为单一问题**：webnovel-writer 已证明「长篇一致性框架 + Anti-AI 词表规则 = 质量达标」，**但它的正文实际由宿主 Claude 生成，不是 DeepSeek**；其代码无任何正文生成 API 接入层，写作智能寄生在 Claude 上。因此风险精确收窄为：*同一套任务书 + 同一份去 AI 味词表，把生成模型从 Claude 换成 DeepSeek，质量掉多少、过不过红线？*——未测。

**可复用 vs 须自建：**

| 能力 | 来源 | 说明 |
|---|---|---|
| 去 AI 味词表规则 | 复用 webnovel-writer `polish-guide.md` | 200+ 词黑名单 + 7 层句式规则，模型自查自改的**软机制**（连词频都非硬门槛，可靠性取决于底层模型是否老实、有文笔） |
| Story System 一致性骨架 | 复用（见 A 节） | 与题材无关，长篇原创刚需 |
| **正文生成 API 接入层** | **须自建** | webnovel-writer 完全没有（仅 embedding/rerank 走可配 API） |
| **锚定用户爱读文风的机制** | **须自建** | webnovel-writer 的 style_sampler.py 只做本书高分章节自我一致性、且未接入写章主链；其文风控制是网文题材套路化（37 题材 + 追读力/爽点），与 Muse 文学向基因相反 |

**换模型后门（承接 Q7）**：DeepSeek 是首选而非唯一。救不动可换模型——BYOK 下换 Claude = 用户自绑 Key 自付高价；托管下换贵模型 = Muse 成本上升，须重算免费额度线（见 C 节）。故生死变量是「文字质量能否达线」，不是「某个特定模型行不行」。

> 盲测的**验证判据、时点、红线定义**属产品级门禁，已上升到主文档 §七正文，不在本附录。

---

## C. 单位经济与成本结构（对应模块 0 模型接入与用量）

**决策（PRFAQ Q4 = B）**：托管为主 + BYOK 进阶。

- **默认·平台托管**：Muse 出 Key，用户无感、零门槛进来即写 → 保住发布稿「一个晚上、不需经验」的承诺。**成本护栏 = 给托管设免费额度上限**（如 N 章/天或 N 次生成/天），顶住烧钱。
- **进阶·BYOK**：用户在设置页绑定自己的 API Key，成本自付 → 给重度用户解绑额度，正好把最烧钱的重度用户成本挪走，缓解单人现金流。

**成本结构提醒（来自 A 节机制）**：写一章非单次调用，是 context-agent 拉取 + 起草 + reviewer 审查 + 去 AI 味润色 + data-agent 提取的**多次调用叠加**，叠加用户反复重写 → 单章真实 token 成本须按「5–10 次调用/章」量级估。**用户尚未精算，属可接受未知。**

**V1 实现前必答小尾巴**：托管免费额度的具体数值（几章/天）取决于盲测（B 节）跑出的「单章真实成本」，须在写模块 0 用量能力前定下。

**边界**：BYOK 只解决「不为每个活跃用户净亏」，**不等于盈利模式**；正式定价/商业模式仍待定（见 E 节）。

---

## D. 护城河与竞争态势（战略，不落 PRD 正文能力）

**决策（PRFAQ Q6）**：诚实定性——单人阶段几乎无传统技术护城河（DeepSeek 人人可调；一致性机制抄 GPL 开源项目人人可抄；探索/创作流程是产品设计可复刻）。不硬造技术壁垒，采用护城河一 + 二组合：

- **一·定位卡位 + 心智**：「纯读者→创作者」定位目前无人占领（Sudowrite / 番茄 / 阅文全预设「用户本来就想写」）。大厂不做非不会做，是看不上——该人群无痛、变现不明、与其网文主业逻辑相反。护城河 = **大厂战略盲区 + 先占住这群人心智**。
- **二·不争大厂，赌细分小市场**：单人项目服务好一小群「读者转作者」，活成小而美、能自养即赢。

**尾部风险（战略上限，非 V1 生死题）**：护城河一的前提是「大厂盲区」——一旦「读者→创作者」被验证有利可图，盲区消失，大厂可携流量/模型/资金下场，Muse 无技术壁垒抵挡。届时只能靠先发心智 + 用户创作数据沉淀（护城河三，V1 未采用，可作 V2 加固方向）。

---

## E. 残留待定项（须创始人正式拍板，不可用营销话术糊弄）

| 缺口 | 性质 | 护栏 | 状态 |
|---|---|---|---|
| **数据与版权**（是否用于训练、数据使用政策、版权归属条款） | 法律 + 商业承诺 | 承诺不拿私密创作训练模型；作品版权归用户 | 未定（PRFAQ Customer FAQ Q6 占位） |
| **商业模式 / 定价**（订阅 / 免费+增值 / 按用量） | 商业决策 | 避免「写着写着弹付费墙」伤信任 | 未定（BYOK 只保证不净亏，非盈利模式） |

**利好参照（可作数据/版权 FAQ 论据）**：美国版权局 2025 终版——有足够人类创作表达的 AI 辅助作品可版权（case-by-case）。Muse 强调用户掌控设定/选择/修改，落在可版权一侧。

---

## F. 其他须记住的约束与风险

- **撞名风险**：Sudowrite 旗舰模型就叫「Muse」，英文市场混淆风险，建议早评估是否改名 / 加限定词。
- **对外承诺一致性**：「一个晚上写出第一章」「邀请码」是发布稿对外承诺，须与实际产品能力 / 放量策略一致。
- **同人钩子硬边界**：「①把你读到的烂尾帮你重写」= 获客钩子，只在广告投放层，**绝不进产品正式门面**（发布稿/落地页/商店描述），且重写他人 IP 不可公开发布或商业化引流（中美版权+平台反 AI 洗稿约束）。产品内核是 ④ 全原创创作。
- **AI 强制标识**：2025.9.1 起 AI 生成内容强制标识——V1 须在导出件/分享页标注「AI 辅助生成」（属加标识义务，非建平台审核体系）。
