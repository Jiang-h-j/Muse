# Muse 产品需求文档 · PRD V1

- 版本：V1（初版）
- 日期：2026-07-17
- 状态：待评审
- 关联文档：[原型规格](prototype/spec/prototype-spec.md)、[探索页面待实现需求](prototype/spec/exploration-pending-requirements.md)、[原型说明](prototype/README.md)

---

## 一、产品定位

**一句话**：Muse 是一个陪你把一个念头，一章章写成完整小说的 AI 创作伙伴。

- **品牌主张**：让每一个人，成为小说家。
- **解释文案**：你参与设定、选择与修改，Agent 陪你把一个想法，一章章写成小说。
- **核心信念**：Muse 不是"小说生成器"。它不替用户决定故事，而是发掘用户的想法、陪用户一起写。用户全程掌握设定、选择与修改权，成品属于用户。
- **目标用户**：心里有故事想法、爱读小说、想借助 AI 完成长篇创作，但独自硬写会卡壳或坚持不下来的人。
- **成功标准**：用户能用 Muse 真正写出一本自己读得完、认得出是"自己作品"的小说。

> 品牌分层（对标模型梯队）：母品牌 **Muse**；创作 Agent 三档 **Vignette**（速写/轻量）、**Novella**（中篇/主力）、**Saga**（史诗/旗舰）。**当前仅作品牌概念保留，不落成用户可选档位，不在 V1–V3 路线内。**

---

## 二、开发方法论

本节是本 PRD 的组织前提，决定了后面所有能力如何切分与交付。

**1. AI 编程，页面即契约。**
Muse 以 AI 编程方式开发，不手写代码。开发从"锁定 HTML 产品最终形态"出发：原型中的每个页面是前后端契约，后端围绕页面逐个实现能力。**开发的最小单元是页面。**

**2. 现有原型：形态真，数据假。**
当前原型（`prototype/app/`）已跑通全流程页面与交互，但所有内容都是写死的 mock —— 章节正文、故事设定、阶段计划、归档卡片均为固定文本，探索问题是固定数组。
因此**后端开发的本质 = 逐页把 mock 数据替换为真实 AI 能力 + 持久化存储**。页面形态基本不动，换的是"数据从哪来、存到哪去"。

**3. 纵向分模块，横向分版本。**

- **纵向（开发模块）**：从用户使用旅程出发切分能力，落为 6 个开发模块（账户、探索、故事设定、创作、归档、通读交付）。每个模块对应原型中一个或几个页面。旅程与模块并非一一对应，详见第三章。
- **横向（版本）**：每个模块的能力分档迭代，一点点丰富。全局统一三档：

| 版本档    | 名称  | 目标                                |
| ------ | --- | --------------------------------- |
| **V1** | 打通  | 全链路真实跑通：数据真实、能存能取，用户能写出一本"读得完"的小说 |
| **V2** | 好用  | 长篇创作的质量保障：不跑偏、不穿帮、能精修、有文风、能交付成品   |
| **V3** | 精修  | 体验打磨与效率增强                         |

> 注：此处 V1/V2/V3 指**能力版本**（功能丰富程度），与本文档标题的 PRD V1（文档修订版本）无关。

---

## 三、能力全景（纵向）

Muse 的能力有两重身份，本 PRD 显式分开描述，避免用"开发数据块"的切法去讲"用户体验"：

- **用户旅程**：用户会主动"走进去"的站点。这是叙事视角，决定产品的心智模型。
- **开发模块**：各自独立的后端能力 + 数据表。这是工程视角，决定后端如何逐块实现。

两者不是一一对应——有些能力是用户主动走的旅程站点，有些是支撑创作、随时被引用却从不"进入"的常驻层。

### 3.1 用户旅程（体验视角）

用户心智里只有 **3 条主线站点 + 1 个常驻层**：

```
主线（用户会主动走的）:   探索 ──→ 创作（一章接一章地写）──→ 通读 & 交付
                            │              │                      │
常驻层（随时翻查，不"进入"）: └──── 故事档案：设定圣经 + 章节卡片 ─────┘
                                （探索时诞生，创作时被引用 + 自动更新）
```

| 主线站点        | 用户心智                         | 用户目标                 |
| ----------- | ---------------------------- | -------------------- |
| **探索**      | "聊着聊着，故事自己成型了，我改改确认，就开始写了"   | 理清我想写什么，并把它固化成可依据的设定 |
| **创作**（主循环） | "我在写第几章"——读正文、批注、改，满意就定稿写下一章 | 把设定一章一章地写成正文         |
| **通读 & 交付** | "我要去从头读一遍、导出我的书"             | 通读全本、产出可分享的成品        |

- **账户与作品管理**是进入旅程前的"门厅"，不是创作站点，用户无感。
- **故事档案**是常驻层，不是旅程站点：用户从不主动"进入归档"，它在探索时诞生、在创作中被引用、在定稿后自动更新，用户只在想查时翻开它。它把原本割裂在两端的"故事设定"与"章节归档"合成一件事——**我定下的规则**（设定圣经）+ **我已写下的事实**（章节卡片），共同构成长篇防跑偏、防穿帮的 Story Bible。

创作主循环：**规划（幕后）→ 章节创作 → 阶段章节写完 → 下一阶段规划（幕后）**，循环推进。**"阶段规划"对用户不可见**——探索确认设定后，用户体感是直接进入第一章；阶段的切换在幕后完成。用户唯一的方向控制点是**阶段交界处一个极轻、可跳过的"这一段想往哪走？"输入**，也是用户主动提出"进入收尾"的入口。

### 3.2 开发模块（工程视角）

作为后端逐块实现的分解单元，能力仍按下列 6 块独立开发（映射回旅程与原型页面）：

| #   | 开发模块  | 归属旅程        | 对应页面 / 路由                                       | 核心产物        |
| --- | ----- | ----------- | ----------------------------------------------- | ----------- |
| 0   | 账户与作品 | 门厅          | `#/login` `#/register` `#/projects`             | 作品库         |
| 1   | 探索    | 探索（主线）      | `#/projects/:id/explore`                        | 探索对话 + 故事线索 |
| 2   | 故事设定  | 故事档案（常驻层）   | 探索页内设定确认 → 归档页（故事档案统一入口）查阅                      | 设定圣经        |
| 3   | 创作    | 创作（主线）      | `#/projects/:id/chapters/:n`（阶段规划转入幕后，无独立页面）    | 定稿章节        |
| 4   | 归档    | 故事档案（常驻层）   | `#/projects/:id/archive`（故事档案统一入口，含设定圣经 + 章节卡片） | 章节卡片        |
| 5   | 通读与交付 | 通读 & 交付（主线） | 新增（原型暂无）                                        | 可导出的小说      |

> 模块 2 与模块 4 在工程上分别是"设定生成"与"归档生成"两套独立能力和数据表，各自开发；但在用户旅程里它们共同呈现为一个"故事档案"常驻层。第四章按开发模块逐块详述。

---

## 四、各开发模块能力详述

以下按 §3.2 的 6 个开发模块逐块展开。每块标注其在用户旅程中的归属，工程上各自独立实现。

### 模块 0 · 账户与作品管理

> 旅程归属：门厅（非创作站点）

- **目标**：用户可注册登录，集中管理多部作品，随时继续创作；并以零门槛的模型接入让用户进来即写。
- **原型形态**：邮箱密码登录、邀请码注册；作品库支持继续/重命名/删除/新建；含空状态、加载失败、会话过期/登录错误/频控锁定等状态。（*模型接入的设置页 / 用量入口原型暂无，须新增*）
- **能力版本**：
  - **V1**：
    - 真实注册登录与会话；作品的增删改查与持久化；"继续创作"跳转到该作品当前所处步骤。
    - **模型接入（托管为主 + BYOK 进阶）**：默认平台托管——Muse 出 Key，用户零门槛进来即写，保住"一个晚上、不需经验"的承诺；成本护栏 = 托管免费额度上限（N 章/天或 N 次生成/天，**具体数值待盲测跑出单章真实成本后定**，见 §7.2 与技术附录 C）。进阶 BYOK——用户在设置页绑定自有 API Key、成本自付、解绑免费额度，把最烧钱的重度用户成本挪走。
  - **V2**：作品封面/简介、创作进度概览（已写章节数、字数）。
  - **V3**：多端同步、回收站、作品导入。

### 模块 1 · 探索

> 旅程归属：探索（主线站点）

- **目标**：不靠机械问卷，通过有趣、间接的问题发掘用户真正想写的小说。
- **两种模式**（已定，取消表单直接设定）：
  - **引导探索**：系统主导，抛出启发性问题带用户循序推进。
  - **自由探索**：用户主导，直接与 Agent 发散讨论。
  - 两者产出同一套故事设定；在新建作品时二选一，进入后各自独立，不在探索途中切换。
- **原型形态**：
  - **引导探索**：纯选项式沉浸问答，一次聚焦一题；第一题可用一句话自述、其余题提供"都不是这些"自答出口；底部导航栏支持问卷式前后翻页；答完最后一题先进入"整理中"过渡态，再弹出故事设定卡。
  - **自由探索**：连续只读对话记录；右侧"故事线索"区可直接编辑；"给我一些方向"只写入输入框不代答；信息充分后才开放"整理为故事设定"。
- **能力版本**：
  - **V1**：接入真实 Explorer Agent 的多轮对话；对话与线索持久化；引导/自由双模式真实可用。先以有限的问题集跑通，非动态选题。
  - **V2**：动态选题——实现 [EXP-P01 问题原型库与动态选题]、[EXP-P02 以覆盖度决定探索是否完成]：Agent 依据已推断结果、置信度、覆盖度动态选择下一个问题，问题数量与顺序不固定，关键结果覆盖充分后才开放"整理为故事设定"，不再是固定轮数。
  - **V3**：跨作品的用户创作偏好沉淀，复用于新作品探索。

### 模块 2 · 故事设定（设定圣经）

> 旅程归属：故事档案（常驻层）——"我定下的规则"

- **目标**：把探索产物固化为整部小说创作的**唯一依据**，并在长篇中防止设定穿帮。这是长篇创作最痛的点。
- **原型形态**：探索结束弹出设定候选卡片，可直接编辑、可向 Agent 反馈生成新版本；确认后成为只读的全文指导上下文；"回到探索"需二次确认并丢弃当前设定。
- **能力版本**：
  - **V1**：真实生成故事设定，并支持用户编辑确认；确认后作为后续所有创作的上下文注入。
    - **设定字段集（12 项，广谱网文向）**：借鉴开源项目 webnovel-writer 的成熟设定结构、按 Muse 广谱网文定位重塑；**只借"产出物字段结构"，不借"表单式采集流程"**——设定由探索对话渐进聊出，非逐格填表。字段以此处为准（原型六项为暂定未定稿，已被替换）：
      - **通用主干（7，所有题材必备）**：① 题材 genre（决定下方特化字段是否激活）② 核心吸引力（并入"一句话故事 + 核心卖点 + 目标阅读体验"）③ 主角（姓名 + 核心欲望 + 致命缺陷）④ 主要冲突（核心对抗 + 反派镜像：与主角共享欲望却走反路）⑤ 关键世界规则（世界规模 + 硬约束）⑥ 整体气质 ⑦ 开篇钩子（第一章拿什么抓住人）。
      - **题材特化（4，按 genre 激活，不匹配可空）**：⑧ 力量体系（修仙玄幻）⑨ 金手指（系统爽文）⑩ 感情线（言情）⑪ 势力格局（设定重题材）。
      - **Muse 独有（1）**：⑫ 文风锚点 `style_profile`（见下 V1 文风样本锚点，webnovel-writer 无）。
      - 说明：V1 存储层一次建全 12 字段 schema、特化字段可空；V1 探索用有限问题集不保证凑齐特化字段，覆盖度驱动的"必须凑齐才放行"属 V2（EXP-P02）。
    - **文风样本锚点（红线验收前提）**：设定阶段新增文风锚点入口——用户从预置样本库选择或粘贴一段自己爱读的文字，系统抽取作品级 `style_profile`（人称、语气、句式节奏、意象密度、段落长度倾向），作为 §7.1 行为红线"像不像用户要的味道"的验收锚。（*入口原型暂无，须新增*）
  - **V2 · 设定圣经**：从"按章节纵向记录"升级为"按实体横向维护"——人物、地点、世界规则、时间线各成条目；每次写作受其约束，防止人名/规则/时间线穿帮。承接探索的覆盖度/置信度产物。**新增文风锁定**：人称、语气、句式节奏一经设定，写作全程遵循，避免千篇一律的 AI 腔。
  - **V3**：设定变更影响分析（改一条设定，提示哪些已写章节受影响）。
  - **暂缓**：设定卡片版本号 + 逐版 diff 展示（原 spec 机制，MVP 用户感知弱，后置）。

### 模块 3 · 创作

> 旅程归属：创作（主线站点，用户 95% 的时间在此）

- **目标**：把设定一章一章地写成正文，用户全程可干预。**Muse 在内部按"阶段"组织长文、控制上下文，但"阶段"对用户不可见**；用户心智里只有"我在写第几章"。
- **原型形态**：
  - 阶段规划：在幕后生成阶段目标 + 章节骨架（章名 + 一句话纲领），不作为独立用户页面呈现；不预设全书总章数与结局位置。
  - 章节创作：可选填"本章想法" → 生成正文 → 分页阅读 → 段落批注 + 整体点评 → 改进本章 / 重新生成整章 → 定稿。定稿版本成为后续章节的正式上下文。
  - > 注：早期原型曾把"阶段规划问答 + 阶段计划确认"作为显性页面展示给用户；现已转入幕后（见下 V1），这两屏不再作为用户流程保留。
- **能力版本**：
  - **V1**：
    - **首个阶段规划全程幕后**：探索确认设定后，Muse 在后台完成第一阶段规划（阶段目标 + 章节骨架），**不展示给用户**，仅作 Agent 自己的创作输入。用户体感是"探索完立即进入第一章"，中间无问答、无确认弹窗。
    - **盲测门禁前置（launch blocker）**：真实正文生成接入**前**，须先完成文字质量盲测（详见 §7.2）——验证 DeepSeek 在去 AI 味约束下能否过 §7.1 行为红线。**盲测未过及格线不得接入正文生成**。
    - 真实生成章节正文（消费故事设定 + 已定稿章节 + 归档卡片作上下文）；批注、点评、改进、重新生成、定稿全部真实可用并持久化；阶段循环推进（用户无感）。
    - **阶段交界的方向输入（收尾控制点）**：一个阶段章节写完、进入下一阶段前，给一个**极轻、可跳过**的入口——一句话"这一段想往哪走？（或直接继续）"。平时跳过无感；它是用户主动表达"我想开始收尾了"的唯一控制点，不可省略。
  - **V2**：
    - **局部重写**：选中段落/句子级修改，保留其余内容，替代"只能改进/重生整章"。
    - **收尾阶段显式化**：用户在阶段交界方向输入中声明进入收尾后，Agent 转为结局收束 + 伏笔回收模式。
    - **规划可展开（可选）**：默认仍幕后；为想深度掌控的用户提供"查看/干预本阶段规划"的进阶入口，不打断默认的无缝流程。
  - **V3**：多结局/分支尝试、章节级重写历史与回滚、写作节奏建议。

### 模块 4 · 归档（故事档案统一入口）

> 旅程归属：故事档案（常驻层）——"我已写下的事实"。与模块 2 共同构成 Story Bible；归档页即故事档案的统一入口

- **目标**：把每个定稿章节凝练成一张卡片，既供用户快速回顾，也作为后续章节的长期上下文，保证不跑偏。**归档页承载整个故事档案**：设定圣经（模块 2）与章节卡片在同一入口内呈现，用户在此一处即可查阅"我定下的规则 + 我已写下的事实"。
- **原型形态**：章节卡片含"本章发生了什么 / 人物变化 / 新增事实与线索 / 未解决悬念 / 章末状态"；按阶段分组归档。
- **能力版本**：
  - **V1**：定稿后真实生成章节卡片并持久化；写下一章时作为上下文注入。在原归档页基础上调整为故事档案统一入口，纳入设定圣经的查阅。
  - **V2**：归档与模块 2 的设定圣经打通——章节定稿时，其中新增的人物/规则/线索**自动回流**更新设定圣经，无需用户手动确认。
  - **V3**：全书线索/悬念看板，追踪伏笔是否已回收。

### 模块 5 · 通读与交付（新增）

> 旅程归属：通读 & 交付（主线站点，旅程终点）

- **目标**：让用户拿到"一本真正的小说"，完成创作闭环的最后一环。
- **能力版本**：
  - **V1**：全本连续通读视图。
  - **V2**：导出 txt / Markdown / EPUB；作品信息页（书名、简介、字数、章节目录）。
  - **V3**：分享链接、只读在线阅读页。

---

## 五、版本路线图（横向）

按能力版本横切所有开发模块，明确每一档整体交付什么。

| 开发模块    | V1 · 打通                               | V2 · 好用                   | V3 · 精修   |
| ------- | ------------------------------------- | ------------------------- | --------- |
| 0 账户与作品 | 真实登录 + 作品 CRUD + 持久化；托管额度 + BYOK 模型接入 | 进度概览、封面简介                 | 多端同步、回收站  |
| 1 探索    | 真实双模式对话（有限问题集）+ 持久化                   | 动态选题 + 覆盖度判定（EXP-P01/P02） | 跨作品偏好复用   |
| 2 故事设定  | 真实设定生成（12 字段）+ 文风样本锚点 + 确认 + 上下文注入；归档页统一查阅 | 设定圣经 + 文风锁定               | 设定变更影响分析  |
| 3 创作    | 章节真实生成 + 修改定稿循环；首个阶段规划全幕后；阶段交界可跳过方向输入 | 局部重写 + 收尾阶段 + 规划可展开       | 分支尝试、重写回滚 |
| 4 归档    | 真实章节卡片 + 上下文注入；作为故事档案统一入口             | 定稿自动回流设定圣经                | 线索/伏笔看板   |
| 5 通读交付  | 全本通读视图                                | 多格式导出 + 作品信息页             | 分享与在线阅读   |

**V1 总目标（Walking Skeleton）**：全流程真实数据打通，用户能从注册一路写到通读，产出一本读得完的小说。
**V2 总目标**：把"能写完"升级为"写得好、写不跑偏、能交付成品"。
**V3 总目标**：体验与效率打磨。

---

## 六、边界与收敛

- **创作前流程尽量薄**：从"想写"到"看见第一段正文"的路径要短。探索确认设定后无缝进第一章，中间不插入任何问答或确认弹窗。
- **阶段规划全程幕后（V1 起）**：原型的"阶段规划问答 + 阶段计划确认"两屏不再作为用户流程；阶段是 Muse 内部的上下文组织手段，对用户不可见。
- **设定卡片逐版 diff 后置**：非 V1 必需。
- **原型第二阶段归档数据**：仅用于多阶段排版预览，不进入真实创作流程。

---

## 七、文字质量红线与盲测门禁（跨模块门禁）

> 本章是跨模块的质量门禁，约束模块 2（文风锚定）→ 模块 3（正文生成）→ 模块 5（拿得出手的分享/交付）。**行为红线是全项目 launch blocker**：文字过不了红线，产品不予上线。

### 7.1 文字质量行为红线（去 AI 味）

**动机**：目标用户是读了十几年小说、眼光最刁的资深读者，糊弄不了。若产出是"一眼假、翻译腔、正确的废话"这类通用 AI 腔，"这是我写的"身份兑现会崩在质量维度上。AI 味是**生成端**问题，靠用户事后反复批注兜不住，必须在生成侧解决，并有可验证的过关线。

**验收判据 = 风格锚定**：用"像不像用户锚定的文风样本"代替"像不像 AI"。产出须贴着用户在设定阶段选定的 `style_profile`（模块 2 文风样本锚点）的味道。判据落在两根正交轴上：

- **轴 A · 风格贴合（正向，主判据）**：产出像不像用户锚定样本的味道。以**单人盲评**量化——评测者对 N 篇匿名对照样本（不同 Provider 产出混同、评测者不知来源）逐篇三级打分：不像=0 / 出戏=1 / 像=2。
- **轴 B · 去 AI 味（负向，硬底线）**：产出触发去 AI 味词表（复用 webnovel-writer `polish-guide`：200+ 词黑名单 + 7 层句式规则）的程度。两项量化：**黑名单词频**（每千字命中数）与**重度句式套路数**（万能金句结尾 / 强行排比 / "不是 X 而是 Y"对仗滥用等）。

**三档判据表**：

| 档位 | 轴 A 风格贴合 | 轴 B 去 AI 味 | 判定 |
| --- | --- | --- | --- |
| **不及格** | 判"像"篇数 < 2/3，或多数篇"不像" | 出现重度句式套路，或黑名单词频显著高于人类样本 | 任一轴不达底线即不及格 → 阻断上线 |
| **及格线**（上线门槛） | 判"像"（≥1 分且多为 2 分）篇数 **≥ 2/3** | 重度句式套路 **= 0 处**，且黑名单词频 **≤ 锚定样本自身的 1.5 倍**（无样本基准时兜底 ≤ 3 词/千字） | **三条同时满足**方为及格 |
| **理想** | N 篇**全部**判"像"，且读者愿意读下去 | 黑名单词频 ≈ 人类锚定样本的自然水平 | 及格之上的追求，非上线强制 |

> **及格线 = 上述三条同时满足**（风格贴合 ≥2/3 判像 + 重度句式套路 0 处 + 黑名单词频 ≤1.5× 样本）。这是 §7.2 盲测"通过与否"的客观触发条件，也是模块 3 正文生成接入的放行标准。风格贴合是硬条件但只要求多数（≥2/3）通过，去 AI 味两项是不可破的硬底线。

### 7.2 盲测前置（正文接入前的生死验证）

**头号未验证生死假设**：webnovel-writer 已证明"长篇一致性框架 + 去 AI 味词表 = 质量达标"，但其正文实际由宿主 Claude 生成，**不是 DeepSeek**。风险精确收窄为：*同一套写作任务书 + 同一份去 AI 味词表 + 同一 `style_profile`，仅把生成模型从 Claude 换成 DeepSeek，质量掉多少、过不过 §7.1 及格线？*——目前是赌，不是知。

**盲测设计**：

- **时点（钉死）**：模块 3 **真实正文生成接入之前**。这是 launch blocker 的硬时点，不可后置。
- **方法**：Claude-vs-DeepSeek 对照——同一 `style_profile` + 同一去 AI 味词表（临时版）+ 同一任务书，**仅切换 Provider**，产出 N 篇匿名对照样本，按 §7.1 判据盲评。
- **门禁**：记录 DeepSeek 是否达 §7.1 **及格线**以上的明确结论。**通过**→ 放行模块 3 正文生成接入，盲测结论与所用样本/词表/prompt 留档；**不通过**→ 阻断接入，调整方案（换 Provider / 调 prompt / 调词表）重测。
- **换模型后门**：DeepSeek 是首选非唯一。救不动可换模型（BYOK 换 Claude 用户自付高价；托管换贵模型须重算免费额度线）。故生死变量是"文字质量能否达线"，不是"某个特定模型行不行"。

---

## 八、已定决策

以下 5 项已于评审中拍板（2026-07-17）：

1. **故事档案统一入口**：在原归档页基础上调整，把归档页作为故事档案的统一入口，设定圣经与章节卡片在此一处呈现，不新建独立页面。
2. **动态选题放 V2**：探索 V1 先用有限问题集跑通双模式对话，动态选题与覆盖度判定（EXP-P01/P02）放 V2，以减轻 V1 关键路径。
3. **通读 V1、导出 V2**：全本通读视图纳入 V1，多格式导出（txt/Markdown/EPUB）放 V2。
4. **归档自动回流**：章节定稿时，新增的人物/规则/线索自动回流更新设定圣经，不需用户手动确认。
5. **Agent 三档暂不做**：Vignette/Novella/Saga 仅作品牌概念保留，不落成用户可选档位，不在当前路线内。
